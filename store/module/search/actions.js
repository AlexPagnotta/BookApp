import * as costants from './constants'
import axios from 'axios';

/**
* method that execute search
*/
export const searchBook = (searchText: String) => {

  return async (dispatch, getState) => {

    var foundBooks = []; //TODO: Search books
  }
}

